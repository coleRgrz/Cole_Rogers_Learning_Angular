import { HairColourPipe } from './hair-colour.pipe';

describe('HairColourPipe', () => {
  it('create an instance', () => {
    const pipe = new HairColourPipe();
    expect(pipe).toBeTruthy();
  });
});
